---
updated: YYYY-MM-DD
---

# 业务领域划分

<!--
  本文件记录项目的业务领域划分，随业务演进持续更新。
  智能体据此判断代码应该放在哪里、新功能属于哪个领域。

  与 ARCHITECTURE.md 的区别：
  - ARCHITECTURE.md = 技术架构（分层、依赖方向、技术栈），相对稳定
  - 本文件 = 业务领域（领域边界、职责、实体），随业务演进变化

  修改本文件不需要架构 RFC，但需要更新 AGENTS.md 中的导航链接。
-->

## 领域清单

| 领域 | 职责说明 | 代码位置 | 关键实体 |
|------|----------|----------|----------|
| <!-- 领域名 --> | <!-- 这个领域负责什么业务 --> | `<!-- 路径 -->` | <!-- 核心数据模型 --> |

## 领域间关系

<!--
  用 Mermaid 画出领域间的依赖/调用关系。
  帮助智能体理解跨领域变更的影响范围。
-->

```mermaid
flowchart LR
  A["领域 A"] --> B["领域 B"]
  B --> C["领域 C"]
```

## 领域通信规则

- 领域之间不允许循环依赖
- 跨领域通信只能通过 Service 层接口
- 如果需要从领域 A 调用领域 B 的功能，必须通过 B 的 Service 层
